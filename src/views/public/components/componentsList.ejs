<!DOCTYPE html>
<html>
<head>
    <title>Pilih Jenis Components</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
    <%- include('../../partials/navbar') %>
    <div class="max-w-3xl mx-auto mt-12 p-8 bg-white rounded shadow">
        <h2 class="text-2xl font-bold text-center mb-8">Pilih Jenis Components</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            <% for(let i=1; i<=10; i++) { %>
            <a href="/components/<%= i %>" class="block p-6 rounded-lg shadow hover:shadow-lg transition bg-blue-50 hover:bg-blue-100 text-center">
                <span class="text-lg font-semibold text-blue-700">Component <%= i %></span>
                <div class="mt-2 text-gray-600 text-sm">Klik untuk melihat detail component <%= i %></div>
            </a>
            <% } %>
        </div>
    </div>
</body>
</html>
